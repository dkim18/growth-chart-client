{
  "name": "growth-chart-client",
  "version": "0.1.1",  
  "engines": {
    "node": "18.17.1",
     "npm": "9.8.1"
  },
  "private": true,
  "dependencies": {
    "gh-pages": "^2.1.1",
    "react": "^17.0.2",
    "react-dom": "^16.9.0",
    "react-scripts": "^5.0.1"
  },
  "scripts": {
    "start": "react-scripts --openssl-legacy-provider start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ],
  "homepage": "https://dkim18.github.io/growth-chart-client/"
}
